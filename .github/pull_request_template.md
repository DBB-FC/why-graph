## What this changes

<!-- One or two sentences. Link the issue it closes, if any: "Closes #123". -->

## How it was tested

- [ ] `npm test` passes (builds `src/main.js` and runs every suite)
- [ ] `npx eslint src/` shows 0 errors (the official Obsidian linter)
- [ ] Tried in Obsidian, on desktop and, if the change touches the map or the panel, on a phone

## Checklist

- [ ] Every user-visible text goes through `T('…')` and has its English line in `EN`
- [ ] Only the Obsidian API: no `fetch`, no Node APIs, no inline styles
- [ ] It keeps the three promises in `CONTRIBUTING.md` (approval before writing, quotes verified by code, no unrequested network)
- [ ] Screenshots attached if the interface changed
